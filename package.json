{
  "name": "moi-collection-app",
  "version": "1.0.0",
  "description": "Moi Collection Full Stack App",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "install-all": "npm install --prefix server && npm install --prefix client --legacy-peer-deps",
    "build": "npm run install-all && npm run build --prefix client",
    "start": "node server/index.js"
  }
}
